import React from 'react';

const Settings: React.FC = () => {
    return (
        <div>
            <h1>Paramètres de la plateforme</h1>
            {/* Ajoutez ici les options de configuration */}
        </div>
    );
};

export default Settings;